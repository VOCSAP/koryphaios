{
  "name": "koryphaios-companion-shell",
  "version": "0.1.0",
  "private": true,
  "description": "Thin Capacitor Android shell for Koryphaios companion mode (PLAN MB6). Requires Android SDK + Gradle to build; see README.md.",
  "scripts": {
    "add:android": "cap add android",
    "sync": "cap sync",
    "run:android": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
