{
  "name": "koryphaios",
  "version": "0.12.0",
  "description": "Koryphaios — the chorus leader: a desktop orchestrator docking multiple Claude Code agent sessions (peers, roadmap, worktrees, embedded browser) in a single window",
  "main": "out/main/index.js",
  "author": "vocsap",
  "license": "MIT",
  "private": true,
  "bin": {
    "kory": "./bin/launch.js",
    "claude-peers-desk": "./bin/launch.js"
  },
  "scripts": {
    "build:hook": "bun build hooks/desk-backchannel-hook.ts --target=node --outfile=deck-plugin/hooks/desk-backchannel-hook.mjs",
    "build:mcp": "bun build mcp/deck-control-mcp.ts --target=node --outfile=deck-plugin/mcp/deck-control-mcp.mjs",
    "build:design": "bun build design-client/deck-design.ts --target=browser --outfile=deck-plugin/design/deck-design.js",
    "dev": "npm run build:hook && npm run build:mcp && npm run build:design && electron-vite dev",
    "start": "electron-vite preview",
    "build": "npm run build:hook && npm run build:mcp && npm run build:design && electron-vite build",
    "typecheck:node": "tsc --noEmit -p tsconfig.node.json",
    "typecheck:web": "tsc --noEmit -p tsconfig.web.json",
    "typecheck": "npm run typecheck:node && npm run typecheck:web",
    "rebuild": "electron-rebuild -f -w node-pty",
    "postinstall": "electron-rebuild -f -w node-pty || echo \"electron-rebuild skipped (run 'npm run rebuild' manually)\"",
    "package": "npm run build && electron-builder",
    "package:win": "npm run build && electron-builder --win",
    "package:mac": "npm run build && electron-builder --mac",
    "package:linux": "npm run build && electron-builder --linux"
  },
  "dependencies": {
    "node-pty": "1.1.0",
    "qrcode-generator": "^2.0.4",
    "selfsigned": "^5.5.0",
    "ws": "^8.21.1"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.2.0",
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/ws": "^8.18.1",
    "@vitejs/plugin-react": "^5.2.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-web-links": "^0.11.0",
    "@xterm/addon-webgl": "^0.18.0",
    "@xterm/xterm": "^5.5.0",
    "electron": "43.1.1",
    "electron-builder": "^26.15.7",
    "electron-vite": "^5.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.7.0",
    "vite": "^7.0.0",
    "zustand": "^5.0.0"
  }
}
